import styled from "styled-components";

const SelectBlock = styled.div`
  width: 100%;
  min-height: 40px;
`;

const Title = styled.div`
  font-size: 20px;
  margin-right: 20px;
`;
export default {
  SelectBlock,
  Title,
};